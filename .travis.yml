before_install:
  - yes | sudo add-apt-repository ppa:hansjorg/rust
  - sudo apt-get update
install:
  - sudo apt-get install rust-nightly python3
script:
  - make debug
  - make release
  - make test
  - make bench
